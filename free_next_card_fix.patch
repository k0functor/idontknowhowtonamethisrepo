diff -ruN game_orig/CMakeLists.txt game_inspect/CMakeLists.txt
--- game_orig/CMakeLists.txt	2026-05-26 19:03:22.000000000 +0000
+++ game_inspect/CMakeLists.txt	2026-05-26 16:59:00.415586815 +0000
@@ -209,6 +209,8 @@
     src/combat/BlockSystem.cpp
     src/combat/EnergySystem.hpp
     src/combat/EnergySystem.cpp
+    src/combat/CardCost.hpp
+    src/combat/CardCost.cpp
     src/combat/EffectSystem.hpp
     src/combat/EffectSystem.cpp
     src/combat/CardPlayValidator.hpp
diff -ruN game_orig/data/statuses/combat_statuses.json game_inspect/data/statuses/combat_statuses.json
--- game_orig/data/statuses/combat_statuses.json	2026-05-26 17:09:22.000000000 +0000
+++ game_inspect/data/statuses/combat_statuses.json	2026-05-26 16:59:00.419024875 +0000
@@ -14,6 +14,13 @@
     "duration_rule": "persistent_combat"
   },
   {
+    "id": "free_next_card",
+    "name": "status.free_next_card.name",
+    "description": "status.free_next_card.description",
+    "type": "buff",
+    "duration_rule": "persistent_combat"
+  },
+  {
     "id": "weak",
     "name": "status.weak.name",
     "description": "status.weak.description",
@@ -64,4 +71,4 @@
     "type": "buff",
     "duration_rule": "persistent_combat"
   }
-]
\ No newline at end of file
+]
diff -ruN game_orig/src/combat/CardCost.cpp game_inspect/src/combat/CardCost.cpp
--- game_orig/src/combat/CardCost.cpp	1970-01-01 00:00:00.000000000 +0000
+++ game_inspect/src/combat/CardCost.cpp	2026-05-26 16:58:57.692067354 +0000
@@ -0,0 +1,49 @@
+#include "CardCost.hpp"
+
+namespace {
+constexpr const char* freeNextCardStatusId = "free_next_card";
+}
+
+namespace CardCost {
+
+bool consumesFreeNextCard(
+    const CombatState& state,
+    const EntityId source,
+    const CardDefinition& definition
+) {
+    if (definition.energyCost <= 0) {
+        return false;
+    }
+
+    if (!state.hasEntity(source)) {
+        return false;
+    }
+
+    return state.entity(source).statuses.has(freeNextCardStatusId);
+}
+
+int effectiveEnergyCost(
+    const CombatState& state,
+    const EntityId source,
+    const CardDefinition& definition
+) {
+    if (consumesFreeNextCard(state, source, definition)) {
+        return 0;
+    }
+
+    return definition.energyCost;
+}
+
+void consumeFreeNextCard(
+    CombatState& state,
+    const EntityId source,
+    const CardDefinition& definition
+) {
+    if (!consumesFreeNextCard(state, source, definition)) {
+        return;
+    }
+
+    state.entity(source).statuses.add(freeNextCardStatusId, -1);
+}
+
+}
diff -ruN game_orig/src/combat/CardCost.hpp game_inspect/src/combat/CardCost.hpp
--- game_orig/src/combat/CardCost.hpp	1970-01-01 00:00:00.000000000 +0000
+++ game_inspect/src/combat/CardCost.hpp	2026-05-26 16:58:57.673940719 +0000
@@ -0,0 +1,27 @@
+#pragma once
+
+#include "cards/CardDefinition.hpp"
+#include "combat/CombatState.hpp"
+#include "entities/EntityId.hpp"
+
+namespace CardCost {
+
+int effectiveEnergyCost(
+    const CombatState& state,
+    EntityId source,
+    const CardDefinition& definition
+);
+
+bool consumesFreeNextCard(
+    const CombatState& state,
+    EntityId source,
+    const CardDefinition& definition
+);
+
+void consumeFreeNextCard(
+    CombatState& state,
+    EntityId source,
+    const CardDefinition& definition
+);
+
+}
diff -ruN game_orig/src/combat/CardPlaySystem.cpp game_inspect/src/combat/CardPlaySystem.cpp
--- game_orig/src/combat/CardPlaySystem.cpp	2026-05-26 17:09:22.000000000 +0000
+++ game_inspect/src/combat/CardPlaySystem.cpp	2026-05-26 16:59:00.417297561 +0000
@@ -1,6 +1,7 @@
 #include "CardPlaySystem.hpp"
 
 #include "cards/CardKeyword.hpp"
+#include "combat/CardCost.hpp"
 
 #include <algorithm>
 #include <stdexcept>
@@ -41,7 +42,9 @@
         return {false, validation.reason};
     }
 
-    energySystem_.spend(state, request.source, definition.energyCost);
+    const int energyCost = CardCost::effectiveEnergyCost(state, request.source, definition);
+    energySystem_.spend(state, request.source, energyCost);
+    CardCost::consumeFreeNextCard(state, request.source, definition);
 
     EffectContext context;
     context.source = request.source;
diff -ruN game_orig/src/combat/CardPlayValidator.cpp game_inspect/src/combat/CardPlayValidator.cpp
--- game_orig/src/combat/CardPlayValidator.cpp	2026-05-26 17:09:22.000000000 +0000
+++ game_inspect/src/combat/CardPlayValidator.cpp	2026-05-26 16:59:00.416673668 +0000
@@ -1,6 +1,7 @@
 #include "CardPlayValidator.hpp"
 
 #include "cards/CardKeyword.hpp"
+#include "combat/CardCost.hpp"
 
 #include <algorithm>
 
@@ -30,7 +31,9 @@
         return {false, "Card is not in hand"};
     }
 
-    if (!state.resources.canSpendEnergy(source, definition.energyCost)) {
+    const int energyCost = CardCost::effectiveEnergyCost(state, source, definition);
+
+    if (!state.resources.canSpendEnergy(source, energyCost)) {
         return {false, "Not enough energy"};
     }
 
diff -ruN game_orig/src/preview/CardPreviewSystem.cpp game_inspect/src/preview/CardPreviewSystem.cpp
--- game_orig/src/preview/CardPreviewSystem.cpp	2026-05-26 17:09:22.000000000 +0000
+++ game_inspect/src/preview/CardPreviewSystem.cpp	2026-05-26 16:59:00.418052482 +0000
@@ -1,6 +1,7 @@
 #include "CardPreviewSystem.hpp"
 
 #include "combat/EffectContext.hpp"
+#include "combat/CardCost.hpp"
 
 CardPreviewSystem::CardPreviewSystem(
     const CardDatabase& cardDatabase,
@@ -27,7 +28,7 @@
     CardPreview preview;
     preview.cardInstanceId = cardInstanceId;
     preview.cardDefinitionId = definition.id;
-    preview.energyCost = definition.energyCost;
+    preview.energyCost = CardCost::effectiveEnergyCost(state, source, definition);
 
     const CardPlayValidationResult validation = validator_.validate(
         state,
